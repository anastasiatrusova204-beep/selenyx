# EXPERTS.md — Экспертные рекомендации для tg-app Mini App

> Источник: RESEARCH.md, экспертная оценка (4 эксперта).
> Задача: реализовать в `tg-app/` standalone-версии Mini App.
> Только то, что касается интерфейса и UX — без бота и лендинга.

---

## 🔴 КРИТИЧНО — делать в первую очередь

### 1. Онбординг: переформулировать первый слайд
**Было:** «Selenyx — лунный навигатор»
**Стало:** «Бывает: день начинается в рассеянности. Selenyx помогает понять, с чего начать.»

Слайды онбординга должны говорить болью пользователя, не функцией продукта.

**Файл:** [index.html](index.html) — `#ob-slide-0`, тексты `.ob-title` и `.ob-text`

---

### 2. Онбординг: добавить кнопку «Пропустить»
Для пользователей, которые уже поняли идею. Сейчас нет способа перейти сразу к знаку.

**Файл:** [index.html](index.html) — добавить `<button id="ob-skip">` рядом с точками
**Файл:** [app.js](app.js) — `initOnboarding()`, обработчик `ob-skip` → `showObSlide(2)`

---

### 3. Мотивация к выбору знака
**Было:** «Выбери свой знак» + «Это нужно для персональных подсказок»
**Стало:** «Выбери знак — и сразу получишь персональный прогноз»

**Файл:** [index.html](index.html) — `#ob-slide-2`, текст `.ob-text`

---

### 4. Подтверждение после каждого сохранения
После выбора знака в онбординге — нет визуального подтверждения что сохранено.
После смены настроек — только toast, но `tg.showPopup` надёжнее на iOS.

Уже реализовано частично (toast). Добавить в `finishOnboarding()`:
```javascript
tg.showPopup({ message: 'Знак сохранён ✓ Прогноз готов!' }, () => {});
```

**Файл:** [app.js](app.js) — `finishOnboarding()`

---

### 5. Обработка 401 и пустых экранов
Сейчас если данные не загрузились — пустой экран без объяснений.
Нужно: показывать сообщение «Не удалось загрузить — обнови страницу» + кнопку retry.

Особенно критично при плохом интернете. В standalone-версии (без API) — проверить что `calcMoonData()` всегда возвращает данные даже при ошибке.

**Файл:** [app.js](app.js) — все `applyXxxData()` функции, добавить try/catch с fallback

---

### 6. Touch target домен-кнопок: минимум 44px
Текущие `.domain-btn` могут быть менее 44px высотой → трудно нажать на iPhone.

**Файл:** [style.css](style.css) — `.domain-btn { min-height: 44px }` ← уже стоит, проверить на реальном устройстве.

---

## 🟡 ВАЖНО — влияет на retention

### 7. Иерархия в «Мой день»: главное крупнее
Сейчас все карточки одного размера. Визуальный акцент должен быть на фазе Луны и главном совете — они крупнее, остальное мельче.

Предлагаемая иерархия:
1. **Tile-row** (фаза + знак) — крупно, уже есть ✅
2. **Главный совет дня** (accent-card «Сегодня хорошо/избегай») — выделить золотой рамкой или акцентным фоном
3. **Подсказка дня** — средний размер
4. **Цвет + число** — компактно, уже есть ✅
5. **Домены** — ниже сгиба, дополнительный контент

**Файл:** [style.css](style.css) — `.accent-card` добавить `border-left: 3px solid var(--gold)`

---

### 8. Haptic feedback на все ключевые действия
Уже есть: выбор вкладки, выбор знака, оракул.
Не хватает: выбор домена (здоровье/работа/etc.) в «Мой день».

**Файл:** [app.js](app.js) — в обработчике `.domain-btn click` добавить `tg.HapticFeedback.impactOccurred('light')`

Уже добавлено в `renderDomainContent`? Проверить.

---

### 9. Retention hook после первого прогноза
Сразу после загрузки «Мой день» — через 3 секунды показывать мягкий баннер «Получай каждое утро 📲» с кнопкой перехода в настройки уведомлений.

Момент максимального вовлечения = сразу после первого просмотра прогноза. Не в настройках.

**Файл:** [app.js](app.js) — в `applyTodayData()`, через `setTimeout(showRetentionHint, 3000)`
**Файл:** [index.html](index.html) — добавить `#retention-banner` с анимацией slide-up

---

### 10. `tg.CloudStorage` вместо `localStorage` для знака пользователя
`localStorage` очищается при обновлениях Telegram клиента на iOS. `tg.CloudStorage` синхронизируется между устройствами и надёжнее.

```javascript
// Сохранение
tg.CloudStorage?.setItem('userSign', sign, () => {});
// Чтение
tg.CloudStorage?.getItem('userSign', (err, val) => { if (val) userSign = val; });
```

Нужен fallback на `localStorage` если `CloudStorage` недоступен.

**Файл:** [app.js](app.js) — `initSplash()`, `finishOnboarding()`, `saveSettings()`

---

### 11. `viewportChanged` для iOS клавиатуры
На iOS при открытии клавиатуры (форма натальной карты) viewport меняется и layout ломается.

```javascript
tg.onEvent('viewportChanged', ({ isStateStable }) => {
  if (isStateStable) document.body.style.height = tg.viewportStableHeight + 'px';
});
```

**Файл:** [app.js](app.js) — после `tg.expand()`

---

## 🟢 ЖЕЛАТЕЛЬНО — виральность и доверие

### 12. Научный дисклеймер в «Мой день»
Одна строка под tile-row: «Данные: Swiss Ephemeris (NASA JPL)»
Снимает скептицизм. Ни один конкурент не выносит это в интерфейс.

**Файл:** [index.html](index.html) — добавить `<p class="science-note">` после tile-row в tab-pane today

---

### 13. Слово «астрология» убрать из первого экрана
Для скептичной аудитории «астрология» = -50% доверия на старте.
Использовать: «лунный навигатор», «ежедневный компас», «ритм дня».

Проверить: `#ob-slide-0`, `#splash-screen`, заголовки вкладок.

---

### 14. Кнопка «Поделиться» не только в совместимости
Добавить шаринг результата оракула:
```
«🥠 Оракул говорит: "[текст предсказания]"
Узнай своё послание → @Selenyx_mybot»
```

**Файл:** [app.js](app.js) — в `revealFortune()`, после показа текста

---

## Технические ограничения — держать в голове

| Проблема | Что сделать |
|----------|------------|
| initData устаревает через 24ч | При 401 → кнопка «Переоткрыть приложение» |
| Telegram Desktop кэширует | При деплое добавлять `?v=TIMESTAMP` к URL |
| Нет DevTools в production | `window.onerror` → `tg.showAlert(err.message)` |
| iOS viewport при клавиатуре | `viewportChanged` handler (пункт 11) |
| Google Fonts медленно | `rel="preconnect"` уже есть ✅ |

---

## Что уже реализовано ✅

- Haptic feedback на вкладки, знак, оракул
- `tg.BackButton` для настроек
- `sessionStorage` кэш с дата-суффиксом (автоинвалидация)
- `tg.showPopup` при сохранении настроек
- `tg.ready()` в init
- `color-scheme` и dark mode
- `touch-action: manipulation` (нет задержки double-tap)
- `env(safe-area-inset-*)` для iPhone
- `inputmode=numeric` + автоформат дат
- `prefers-reduced-motion`
- 44px min-height на кнопках

---

## Приоритет реализации

```
Спринт 1 (сейчас):  пп. 1, 2, 3, 4 — онбординг + подтверждения
Спринт 2:           пп. 7, 9, 14    — иерархия + retention + шаринг
Спринт 3:           пп. 10, 11      — CloudStorage + viewport
После MVP:          пп. 12, 13      — дисклеймер + текст
```
